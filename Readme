def square_digits(num):
    a = 0
    b = ''
    num = str(num)
    for i in num:
        i = int(i)
        a = i ** 2
        a = str(a)
        b = b + a
        a = 0
    return int(b)

test.assert_equals(square_digits(9119), 811181)
