def spin_words(sentence):
    a = ''
    sentence = sentence.split()
    for i in sentence:
        if len(i) < 5:
            a = a + i + ' '
        else:
            a = a + i[:: -1] + ' '
    return a[:-1]

test.assert_equals(spin_words("Welcome"), "emocleW")
